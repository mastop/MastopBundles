{% extends "::backend.html.twig" %}

{% block content %}
    <h1>Tem certeza que deseja deletar o menu: {{ name }}?</h1>
    <a href="{{path('_menu_admin_delete', {'id': id})}}">
        Sim
    </a>
    |
    <a href="{{path('_menu_admin')}}">
        Nao
    </a>
    <br />
    <a href="{{ path('_menu_admin')}}">Listar menus!</a>
{% endblock %}

{% set code = code(_self) %}